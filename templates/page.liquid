<div class="mv-page">
  <h1 class="mv-page-title">{{ page.title }}</h1>
  <div class="mv-page-body rte">{{ page.content }}</div>
</div>

<style>
  .mv-page{ max-width:760px; margin:0 auto; padding:160px 24px 120px; }
  .mv-page-title{ font-family:'Abril Fatface',serif; font-weight:400; color:#ECE9E2; font-size:clamp(38px,6vw,68px); line-height:1.04; margin:0 0 28px; }
  .mv-page-body{ font-family:'Fira Sans Condensed',sans-serif; color:#9b988f; font-size:17px; line-height:1.85; }
  .mv-page-body p{ margin:0 0 18px; }
  .mv-page-body a{ color:#ECE9E2; text-decoration:underline; }
  .mv-page-body strong{ color:#ECE9E2; }
  .mv-page-body h2,.mv-page-body h3{ font-family:'Fira Sans Condensed',sans-serif; color:#ECE9E2; font-weight:700; margin:34px 0 12px; }
  .mv-page-body ul,.mv-page-body ol{ margin:0 0 18px; padding-left:22px; }
  .mv-page-body li{ margin:0 0 8px; }
  @media (max-width:600px){ .mv-page{ padding:120px 20px 90px; } }
</style>
